/**
 * Client-safe application constants. No environment or secret dependencies, so
 * these are safe to import from any layer, including client components.
 */
export const APP_NAME = "Stroman OS";
export const APP_VERSION = "0.1.0";
