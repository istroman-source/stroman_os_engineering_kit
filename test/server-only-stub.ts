// Test stub for the `server-only` package. In production, importing `server-only`
// from client code is a build error; under Vitest (Node) we alias it to this
// no-op so server modules can be unit-tested directly.
export {};
